const theme = require("../theme.js");

module.exports = {
    name: "blankCmd",
    aliases: [],
    needsGameDefn: false,
    needsGameInit: false,
    execute: (sid, cid, pid, args, msg, games, game) => {
        
    }
}
